#!/usr/bin/env node
"use strict";
const argv = require("minimist")(process.argv.slice(2));
console.log("🕊");
if (argv.serve)
    require("./serve");
if (argv.build)
    require("./compile");
if (argv.test)
    require("./test");
if (argv.rename)
    require("./rename");
if (argv.deploy)
    require("./deploy");
//# sourceMappingURL=index.js.map