<tr>
  <td><%= ticket.passport_id %></td>
  <td><%= link_to 'Show', ticket_path(ticket) %></td>
  <td><%= button_to 'Destroy', ticket_path(ticket), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>